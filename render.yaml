services:
- type: web
  name: coinix-faucet
  env: node
  region: frankfurt
  plan: free
  buildCommand: npm install
  startCommand: node api/index.js
  envVars:
  - key: NODE_ENV
    value: production
  - key: PORT
    value: 10000
