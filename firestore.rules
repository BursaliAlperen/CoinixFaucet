rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write only through Admin SDK (server-side)
    // All client-side access is denied by default
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
